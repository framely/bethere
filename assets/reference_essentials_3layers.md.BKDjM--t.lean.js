import{_ as t,c as a,a1 as o,o as n}from"./chunks/framework.B0cMFUTE.js";const u=JSON.parse('{"title":"Focus on Services, Not Conversations","description":"","frontmatter":{},"headers":[],"relativePath":"reference/essentials/3layers.md","filePath":"reference/essentials/3layers.md","lastUpdated":1737412446000}'),i={name:"reference/essentials/3layers.md"};function s(r,e,c,l,h,d){return n(),a("div",null,e[0]||(e[0]=[o('<h1 id="focus-on-services-not-conversations" tabindex="-1">Focus on Services, Not Conversations <a class="header-anchor" href="#focus-on-services-not-conversations" aria-label="Permalink to &quot;Focus on Services, Not Conversations&quot;">​</a></h1><p>Businesses allocate resources to develop conversational user interface (CUI) applications like chatbots and agents because they aim to provide conversational access to their services. Conversational interfaces enable users to interact with the business through natural language, making the process more intuitive and accessible.</p><p>In this context, a service represents a specific business capability or functionality that delivers value to users. These services are often implemented as a collection of Application Programming Interfaces (APIs), which are functions that can be invoked by other software systems. Triggering APIs involve the exchange of data between different systems or components, to make sure the data is well-formed, consistent, and interoperable, each API function is defined by a schema that specifies its input and output parameters along with their respective data types.</p><p>To trigger a function, we need to prepare the values for each parameter according to their respective types, which amounts to creating values of a compound type. The goal of a conversational user interface (CUI) is thus to figure out which service the user prefers. For the type corresponding to that service API, the CUI needs to gather the user&#39;s preferences for each required attribute through conversation and create an object of that type. Of course, if a slot or parameter is also of a compound type, the CUI needs to be applied recursively.</p><p>This perspecive suggests that we can develop CUI, both components and applications, in a schema-grounded approach, which can be defined in three steps:</p><h3 id="declare-schema" tabindex="-1">Declare schema <a class="header-anchor" href="#declare-schema" aria-label="Permalink to &quot;Declare schema&quot;">​</a></h3><p>To create an object for a specific type conversationally, one must first declare the type on the platform. For input, OpenCUI supports both primitive types, such as entities, and compound types, such as frames and skills. It also supports one output type: dialog acts.</p><p>Entity: Represents a basic or primitive category of data that is captured during a conversation. It defines the data type and how that data can be used or understood by the system. For example, an entity could represent concepts like &quot;date&quot;, &quot;location&quot;, and &quot;time&quot;.</p><p>Both frames and skills are compound types, where one need to specify all their slots (a common term for attributes in the community) on the platform, including their labels and CUI types. A frame is only responsible for collecting user preferences per that type, a skill also comes with definition for what to do after the user&#39;s preferences are collected.</p><p>Instead of supporting only entity-typed slots, OpenCUI also supports slot of compound type, list-contained types, and even polymorphic types. By covering all the static types used by OpenAPI, it becomes easier to build CUI to interact with all the functions that can be defined by widely adopted OpenAPI.</p><h3 id="attach-interaction-annotation" tabindex="-1">Attach interaction annotation <a class="header-anchor" href="#attach-interaction-annotation" aria-label="Permalink to &quot;Attach interaction annotation&quot;">​</a></h3><p>If the user does not provide all the necessary information at once or changes their mind during the conversation, the chatbot must guide the conversation to gather the required user preferences before invoking the service API. To maintain a consistent user experience across different languages, the chatbot needs to follow a language-independent, programmable interaction logic.</p><p>On OpenCUI, the interaction logic is implemented through a statechart, programmed by attaching annotations directly to the slots. For each primitive slot, its state machine-based interaction is defined by a couple of annotations, each corresponding to a specific question:</p><ol><li>Whether this slot can be initialized and how, based on the information collected so far;</li><li>Whether we need to propmt user for this slot, what is the condition for doing that;</li><li>Whether and how a list of candidate values should be provided to the user for more efficient selection;</li><li>Whether the value supplied by the user is serviceable; for example, the showtime the user preferred might already be sold out.</li><li>Whether the user will have the opportunity to confirm the value they selected.</li></ol><h3 id="attach-language-annotation" tabindex="-1">Attach language annotation <a class="header-anchor" href="#attach-language-annotation" aria-label="Permalink to &quot;Attach language annotation&quot;">​</a></h3><p>If we want to communicate with the user via voice, we need automatic speech recognition (ASR) to convert sound waves into text and text-to-speech (TTS) to convert text back into sound. After this, two language-dependent translation tasks take place: converting the user&#39;s utterance into an event, which is a structured representation of semantics, and generating natural text for a dialog act.</p><p>While large language models (LLMs) are effective for these translation tasks, they are not infallible and can make mistakes. When errors occur in a production system, it is essential to have an efficient way to fix them quickly without undergoing a lengthy fine-tuning process.</p><p>OpenCUI adopts a retrieval-augmented in-context learning approach for dialog understanding. By attaching exemplar annotations to slots, these exemplars are added as in-context examples only when the conversation is about the specific slot, providing a quick and context-dependent way to correct understanding errors. Similarly, the same dialog act can be converted into different natural text variations using template annotations.</p><h3 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h3><p>CUI development is about creating instances of every type required by the backend service APIs that one wants to expose to the user. This insight suggests a schema-guided CUI development approach with the following steps:</p><ol><li>Begin by deciding which services one wants to expose.</li><li>Declare the types required directly and indirectly by these APIs by specifying their schema.</li><li>Add interaction annotations to the slots of these types.</li><li>For each language one want support, add language annotation to the hosting interaction annotations.</li></ol><p>Every declared type, an statechart implementation is generated based on its schemas and annotations. Thanks to these generated statecharts, it is trivial for OpenCUI runtime to keep track of the dialog state. Regardless which conversation path is followed to get to dialog state, given the state, the runtime will pick action based on event converted from user input, with goal to deliver the current service as soon as possible. This factorized way of defining the conversational logic, builder never have to worry about how conversation should flow globally.</p><p>For every declared type, a statechart implementation is generated based on its schemas and annotations. Thanks to these generated statecharts, OpenCUI runtime can easily track the dialog state. Given state, regardless of which conversation path is taken to reach there, the runtime selects the next action based on events converted from user input, always aiming to deliver the intended service as quickly as possible. This factorized approach to defining conversational logic ensures that builders never have to worry about the conversation flows that grow exponentially.</p>',23)]))}const f=t(i,[["render",s]]);export{u as __pageData,f as default};
