import{_ as t,o,c as r,a1 as i}from"./chunks/framework.DH-rCavj.js";const v=JSON.parse('{"title":"Overview","description":"","frontmatter":{},"headers":[],"relativePath":"reference/providers/overview.md","filePath":"reference/providers/overview.md","lastUpdated":1745270718000}'),n={name:"reference/providers/overview.md"};function s(a,e,d,l,p,c){return o(),r("div",null,e[0]||(e[0]=[i('<h1 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h1><p>The goal of OpenCUI is to make it easy to build conversational user interfaces for services. Following best practices, all services on OpenCUI are modeled in two parts: interfaces and implementations. This separation allows producers and consumers of these functionalities to be developed independently. More importantly, conversational behaviors rely only on these interfaces, making it possible to switch between different implementations seamlessly.</p><p>The implementations of interfaces, which we call <strong>providers</strong>, come in two types: <strong>native providers</strong> and <strong>scripted providers</strong>. Native providers are implementations developed in Java or Kotlin. Scripted providers allow builders to use other scripting languages to build (SQL) or connect to (JSON) impelementation.</p><p>Once you have a provider and the corresponding backend running, you need to wire the provider to its service interface in your chatbot. This enables the chatbot to communicate with the backend.</p><h2 id="native-providers" tabindex="-1">Native Providers <a class="header-anchor" href="#native-providers" aria-label="Permalink to &quot;Native Providers&quot;">​</a></h2><p>Native providers are developed as <strong>extensions</strong>, which are software modules designed to supply one or more providers to the platform. Since OpenCUI is implemented in Kotlin, these extensions are developed as standard Kotlin modules using conventional toolchains like Gradle and the popular Spring Boot framework. Creating an extension involves four simple steps:</p><ol><li>Define the interface on the OpenCUI platform (builders can only do this at the application level for services).</li><li>Generate code stubs for these interfaces as a basis for implementation.</li><li>Develop the extension. You are encouraged to contribute your extension to <code>opencui/extensions</code>.</li><li>Register each provider developed in the extension on the platform so it can be wired to chatbots and supply its implementation to them.</li></ol><p>Native providers can be registered as <strong>external</strong>, in which case the builder does not need to make the source code available to the OpenCUI platform. However, if a chatbot relies on even one external provider, it cannot be hosted by OpenCUI. Instead, the builder must export the generated Kotlin project and handle the build and deployment process according to their own DevOps practices. Regardless of whether providers are external, extension builders must register their providers on the platform so that OpenCUI can generate the corresponding frontend code.</p><p>For more details, see <a href="./native.html">Native Provider</a>.</p><h2 id="scripted-providers" tabindex="-1">Scripted Providers <a class="header-anchor" href="#scripted-providers" aria-label="Permalink to &quot;Scripted Providers&quot;">​</a></h2><p>Sometimes, the backend implementation of a service is accessible through a scripting language like SQL or JSON rather than native code. Providers for these backends, known as <strong>scripted providers</strong>, come in two types: <strong>hosted providers</strong> and <strong>stub providers</strong>. In practice, these scripted providers are just native providers that expose some interface for further customization.</p><ul><li>OpenCUI currently supports one hosted provider: <strong>PostgreSQL</strong>. When we say &quot;hosted,&quot; we mean that OpenCUI aware the backend implementation, including the database, tables, and SQL-based function logic.</li><li>For <strong>stub providers</strong>, OpenCUI does not host any data or have explicit knowledge of the data schema. Instead, OpenCUI facilitates function invocation through these providers. Many stub provider types will be available, including Web API provider.</li></ul><p>Creating scripted providers typically involves three steps:</p><ol><li>Define the service-specific interface on the platform.</li><li>Choose the provider type. This determines how the actual data source is accessed and which scripting language is used for implementing each function in the service interface.</li><li>Implement the service functions using the scripting language required by the provider type. Functions can also be implemented in Kotlin—these are known as <strong>native functions</strong>.</li></ol><p>See <a href="./postgrest.html">PostgreSQL Provider</a> for more details. For now, PostgreSQL provider can be hosted by OpenCUI or Supabase. Pick Supabase for added reliability.</p>',15)]))}const u=t(n,[["render",s]]);export{v as __pageData,u as default};
