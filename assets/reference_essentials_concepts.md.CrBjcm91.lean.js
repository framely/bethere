import{_ as t,c as a,a2 as n,o}from"./chunks/framework.BO4SuroD.js";const u=JSON.parse('{"title":"Type systems​","description":"","frontmatter":{},"headers":[],"relativePath":"reference/essentials/concepts.md","filePath":"reference/essentials/concepts.md","lastUpdated":1737499785000}'),i={name:"reference/essentials/concepts.md"};function s(l,e,r,c,d,h){return o(),a("div",null,e[0]||(e[0]=[n('<h1 id="type-systems​" tabindex="-1">Type systems​ <a class="header-anchor" href="#type-systems​" aria-label="Permalink to &quot;Type systems​&quot;">​</a></h1><p>To invoke a function, we need to create an object of that function&#39;s type.</p><p>To do this, OpenCUI allows you to define CUI types (also known as components), such as skills (think of functions), frames (needed by their parameters), and entities (primitive types), in three steps. First, declare the type and its component, which will be mapped to the hosting language&#39;s data types (currently Java/Kotlin) so that they can be used to invoke service functions. Second, use dialog annotations to define interaction logic. Lastly, use exemplars and templates to control how natural text is converted to structured representation, and vice versa.</p><p>On OpenCUI, all skills are implementations of a special runtime interface IIntent, thanks to the built-in polymorphism support. Furthermore, every chatbot is started with a built-in skill that has a slot of <code>List&lt;IIntent&gt;</code> type. As a result, slot filling, or instantiating the slot type, becomes one of the most important aspects that chatbot builders need to consider.</p><h3 id="skills​" tabindex="-1">Skills​ <a class="header-anchor" href="#skills​" aria-label="Permalink to &quot;Skills​&quot;">​</a></h3><p>Generally, a skill is essentially a function that a user can access through conversations. As a CUI data type for functions, it is designed to define a self-contained conversational component that delivers some functionality to a user. This means that all three aspects of conversational service delivery need to be defined on top of the corresponding data type:</p><p>Collect what the user wants through slot filling. You can add slots in the Slots section of a skill. Invoke functions using the collected slot value as an input parameter. The invoked function can be a native function defined in the current skill, or a function from the slot of the current skill, in a nested sense like slot.function(). You can add service slots in the Services section of a skill. Verbalize the service result and render it in the channel. At the language level, skills can be expressed mainly by verb phrases or full sentences. When expressed in a full sentence, the subject needs to be in the first person. Examples of such utterances include: &quot;Book me a table for two for Sunday evening&quot; or &quot;I would like to make a reservation on Sunday&quot;.</p><h3 id="frames​" tabindex="-1">Frames​ <a class="header-anchor" href="#frames​" aria-label="Permalink to &quot;Frames​&quot;">​</a></h3><p>In OpenCUI, a frame is a standard object-oriented class type with support for composition and polymorphism behaviors. Frames typically map to parameter types for your function at schema level.</p><p>With inheritance, we can easily support conversations like &quot;What symptoms do you have?&quot; by defining an interface symptom frame and multiple concrete frames, each for an actual symptom. Since each concrete frame can have different interaction logic, when we try to fill an interface Frame slot, we can naturally get the conversational experience we need.</p><p>At the language level, a Frame represents objects with properties and is typically expressed in a noun phrase such as &quot;large, spicy noodle&quot;.</p><h3 id="entities​" tabindex="-1">Entities​ <a class="header-anchor" href="#entities​" aria-label="Permalink to &quot;Entities​&quot;">​</a></h3><p>Entity is your primitive type in OpenCUI, and it is basic building block for complex data type. Entity type can have subtypes. For example, cell phone models could be partitioned into feature phone and smartphone, and smartphone can be further partitioned to iPhone and android phones.</p><p>For each entity type, there are many entity entries. Each entity entry provides a set of expressions that are considered to be trigger for that entry, or when one of expression is mentioned, we consider user prefer the corresponding entry.</p><h3 id="dialog-acts​" tabindex="-1">Dialog acts​ <a class="header-anchor" href="#dialog-acts​" aria-label="Permalink to &quot;Dialog acts​&quot;">​</a></h3><p>Dialog act is another CUI data type in OpenCUI and is designed to help map structured meaning back to natural text.</p><h2 id="annotations" tabindex="-1">Annotations <a class="header-anchor" href="#annotations" aria-label="Permalink to &quot;Annotations&quot;">​</a></h2><p>After these types are defined at schema level, builder can add annotation on top of it to control the every aspect of this component. An example will be what if a user did not specify a value for a required slot, how do we prompt them in a given language.</p><h3 id="dialog-annotations" tabindex="-1">Dialog annotations <a class="header-anchor" href="#dialog-annotations" aria-label="Permalink to &quot;Dialog annotations&quot;">​</a></h3><p>In cases where users do not provide all the information needed in a single utterance, you need to design a conversation to help chatbot get the user&#39;s preference for a given option. This can be done in OpenCUI by adding various dialog annotations.</p><p>Dialog annotations can be defined both on slot and type level. Slot level annotations defines how individual slot can be filled. This includes whether the slot can take multiple values, whether it needs confirmation. For frame slot, whether the polymorphism is allowed. Type level annotations are related to multi-slot filling where values for slots need to collectively make business sense. This includes annotations like value recommendations and value check. Value recommendation provides a user with candidate list so that they can pick one from that instead of input something that is invalid. Value check makes sure agent catch user input error as early as possible so that conversation can be efficient. Dialog annotations are naturally separated into interaction related and language related, each can be handled by different set of people. This makes multiple language support easy.</p><h3 id="backend-annotations" tabindex="-1">Backend annotations <a class="header-anchor" href="#backend-annotations" aria-label="Permalink to &quot;Backend annotations&quot;">​</a></h3><p>OpenCUI allow you to build hosted SQL provider declaratively using annotations and SQL.</p><ul><li>Declare all data types first required the service APIs and their implementation helper functions;</li><li>Add storage annotation to help OpenCUI infer corresponding table schema;</li><li>Use SQL to implement the service APIs;</li><li>Add backoffice annotation to define admin interface, including look and feel, and input mode.</li></ul><p>Once you have decided on the data frame that you want to persist, you can simply turn on the storage annotation for that frame. When the storage annotation is turned on for a frame, OpenCUI will automatically create a corresponding table with each slot mapped to a column in the hosting database. You can precisely control how these columns are created, using annotations such as:</p><ul><li>Not Null: indicates whether a column can host a null value;</li><li>Default Value: defines the default value for the column;</li><li>Unique: indicates whether a value in the column needs to be unique and can potentially serve as a key.</li></ul><p>The data in these tables also needs to be accessed and manipulated by your operations team. For the SQL provider, they can do so through a web interface called &quot;back office&quot;. The back office is automatically created based on the back office annotation, and its user experience can be controlled by annotations such as:</p><ul><li>Sortable: indicating whether one can sort the entire table by the given column.</li><li>Input Mode: determining whether a dropdown menu can be used for inputting this column and specifying the available choices.</li></ul>',28)]))}const f=t(i,[["render",s]]);export{u as __pageData,f as default};
