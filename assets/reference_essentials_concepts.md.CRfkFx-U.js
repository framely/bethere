import{_ as t,o as a,c as n,a1 as s}from"./chunks/framework.DH-rCavj.js";const m=JSON.parse('{"title":"Type systems​","description":"","frontmatter":{},"headers":[],"relativePath":"reference/essentials/concepts.md","filePath":"reference/essentials/concepts.md","lastUpdated":1737583235000}'),i={name:"reference/essentials/concepts.md"};function o(r,e,l,c,p,d){return a(),n("div",null,e[0]||(e[0]=[s('<h1 id="type-systems​" tabindex="-1">Type systems​ <a class="header-anchor" href="#type-systems​" aria-label="Permalink to &quot;Type systems​&quot;">​</a></h1><p>Triggering APIs involves the exchange of data between different systems or components. Defining these API functions with a type system, such as <a href="https://swagger.io/docs/specification/data-models/" target="_blank" rel="noreferrer">OpenAPI</a>, ensures that the exchanged data is well-formed, consistent, and interoperable.</p><p>By specifying a type for a slot, the function’s capability is limited or extended to process only data that conforms to that type. The types of parameters thus define the scope of what the function can handle. Additionally, features like polymorphism and generics can significantly expand a function’s scope by enabling it to work with multiple types in a controlled manner.</p><p>To invoke an API function conversationally, a chatbot must create an object of that function&#39;s type by interacting with users to capture their preferences. OpenCUI elevates existing OpenAPI data types to the CUI level by attaching dialog annotations to their schemas. The resulting CUI-level type can be thought of as a CUI component, as it not only defines what constitutes a valid instance of the type but also how to create such an instance through conversation. These CUI types can then be used as slot types within larger CUI types, enabling the development of increasingly complex conversational behaviors.</p><p>Instead of supporting only primitive types and simple compound types with primitive-typed slots, OpenCUI supports generic type lists, polymorphism, and nested compound types, making it easy to build conversational interfaces for any OpenAPI-definable service. Specifically, OpenCUI allows the definition of three input types—entities, frames, and skills—and one output type: dialog act.</p><h3 id="entities​" tabindex="-1">Entities​ <a class="header-anchor" href="#entities​" aria-label="Permalink to &quot;Entities​&quot;">​</a></h3><p>An entity is a primitive type at CUI level and serves as the basic building block for more complex data types. Examples for entity includes movie title, time, etc.</p><p>To recognize a value from conversation, an entity requires a recognizer to extract mentions from user utterance and normalize them into a value. A common type of recognizer is the list recognizer, where an entity is defined by multiple entity entries, each associated with a set of expressions that serve as triggers. When a user mentions one of these expressions, it is recognized and interpreted as the user&#39;s preference for the corresponding entity entry. Other types of recognizers include regex-based and LLM-based approaches.</p><p>To model real-world <em>is-a</em> relationships, OpenCUI allows entities to have subtypes, enabling hierarchical categorization. For example, cell phone models can be categorized into feature phones and smartphones, with smartphones further divided into iPhones and Android phones.</p><h3 id="skills​" tabindex="-1">Skills​ <a class="header-anchor" href="#skills​" aria-label="Permalink to &quot;Skills​&quot;">​</a></h3><p>A skill is essentially a function that a user can access through conversations. As a CUI data type for functions, it is self-contained conversational component that delivers some functionality to a user. A skill does the following things:</p><ol><li>Collect what the user wants through slot filling. The slots can be add at schema level.</li><li>Invoke functions using the collected slot value as an input parameter. The invoked function can be a native function defined in the current skill, or a function from the service slot.</li><li>Verbalize the service result and render it for the channel in the response section.</li></ol><p>At the language level, skills can be expressed mainly by verb phrases or full sentences. When expressed in a full sentence, the subject needs to be in the first person. Examples of such utterances include: &quot;Book me a table for two for Sunday evening&quot; or &quot;I would like to make a reservation on Sunday&quot;. One can add exemplar to the skills, and simply redeploy chatbot to hot fix the dialog understanding issues.</p><p>On OpenCUI, all skills are implementations of a special runtime interface IIntent, thanks to the built-in polymorphism support. Furthermore, every chatbot is started with a skill Main that has a slot of <code>List&lt;IIntent&gt;</code> type.</p><h3 id="frames​" tabindex="-1">Frames​ <a class="header-anchor" href="#frames​" aria-label="Permalink to &quot;Frames​&quot;">​</a></h3><p>In OpenCUI, a <em>Frame</em> is a user-defined compound type that supports composition and polymorphism at the CUI level. Frames are typically used as parameter types for functions at the schema level or as attribute types for constructing more complex frames.</p><p>With inheritance, OpenCUI enables natural conversational interactions. For example, a conversation like <em>&quot;What symptoms do you have?&quot;</em> can be modeled by defining an interface <em>Symptom</em> frame along with multiple concrete frames, each representing a specific symptom. Since each concrete frame can have distinct interaction logic, filling an interface slot dynamically adapts to provide the appropriate conversational experience.</p><p>At the language level, a <em>Frame</em> represents objects with properties and is typically expressed as a noun phrase, such as <em>&quot;large, spicy noodle.&quot;</em> Additionally, by adding exemplars to skills and redeploying the chatbot, builders can quickly address and refine dialog understanding issues.</p><h3 id="multi-value" tabindex="-1">Multi-value <a class="header-anchor" href="#multi-value" aria-label="Permalink to &quot;Multi-value&quot;">​</a></h3><p>A slot can be declared as <em>multi-valued</em>, meaning it can hold multiple instances of a given type. At the schema level, this is represented as <code>List&lt;T&gt;</code>.</p><p>When a slot is multi-valued, OpenCUI automatically prompts the user with <em>&quot;Do you want more of $T?&quot;</em> after an instance of <code>T</code> is created. If the user responds affirmatively, the conversation for type <code>T</code> repeats to collect another instance, allowing for a flexible and natural way to gather multiple values.</p><h3 id="dialog-acts​" tabindex="-1">Dialog acts​ <a class="header-anchor" href="#dialog-acts​" aria-label="Permalink to &quot;Dialog acts​&quot;">​</a></h3><p>A <em>Dialog Act</em> is another CUI data type in OpenCUI, designed to map structured meaning back to natural language. OpenCUI supports various dialog acts, such as <em>Inform</em> and <em>Notify</em>.</p><p>To control how a dialog act is verbalized, templates can be added to specify its exact phrasing. These templates can also serve as training examples, enabling the system to generate verbalizations automatically in the future.</p>',24)]))}const u=t(i,[["render",o]]);export{m as __pageData,u as default};
